defaults:
  - _self_
  - agent: ppo
  - env: coffeeshop
  - mediator: default
  - trainer: default

run_id: default_run
dist: false

run:
  steps: 1000
  horizon: 200
  meeting_interval: 400
  seed: 42
