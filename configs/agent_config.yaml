# Agent configuration for CoffeeShop
# Choose a baseline agent and its hyperparameters.

agent:
  # one of: ppo, sac
  name: ppo
  params:
    lr: 0.0003
    gamma: 0.99
    lam: 0.95
    clip_ratio: 0.2
    entropy_coef: 0.0
    value_coef: 0.5
    update_epochs: 4
    minibatch_size: 64
